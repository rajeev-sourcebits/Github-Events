<div id ='inputField'>
  <h1> Enter your Github Username, Thanks </h1>
	<%= form_tag( {:action => 'getUserEvents'}, :id => 'userFeed') do |f|%>
	<p align='center' id='username'>
 		<%= text_field_tag :username, params[:username] %>
	  <%= submit_tag "Fetch" %>
	</p>
	<% end %>
</div>
